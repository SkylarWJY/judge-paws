{
  "name": "judge-paw",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Judge Paw — an AI judge that delivers playful, fair verdicts for couples' disputes.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --env-file=.env server.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.69.0",
    "stripe": "^22.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
